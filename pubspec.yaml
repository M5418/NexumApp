name: nexum_app
description: Nexum Flutter app
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0
  cached_network_image: ^3.3.1
  video_player: ^2.8.2
  visibility_detector: ^0.4.0+2
  readmore: ^3.0.0
  google_nav_bar: ^5.0.6
  image_picker: ^1.0.4
  permission_handler: ^11.3.1
  provider: ^6.1.1
  shared_preferences: ^2.5.3

  # Audio playback
  just_audio: ^0.10.5
  audio_session: ^0.2.2
  audioplayers: 6.5.1

  intl: ^0.20.2
  flutter_pdfview: ^1.3.2
  http: ^1.1.0
  path_provider: ^2.1.2
  pro_image_editor: ^11.5.6
  dio: ^5.9.0
  flutter_secure_storage: ^9.2.4
  
  file_picker: ^8.0.0
  url_launcher: ^6.3.0

  # Audio recording
  record: ^6.1.1
  web: ^1.1.1

  # Web-compatible PDF viewer
  syncfusion_flutter_pdfviewer: 31.1.23
  timeago: ^3.7.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

dependency_overrides: {}

flutter:
  uses-material-design: true